version: '3.8'
services:
  app-core:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - CUBA_CONF_DIR=/app/config
    volumes:
      - ./modules/core/src/com/company/clinic/app-prod.properties:/app/config/app-prod.properties