version: '3.8'

services:
  cuba-app:
    build: .
    image: clinic
    container_name: clinic
    ports:
      - "8080:8080"
      - "8443:8443"
    networks:
      - cuba-network
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      # Configuración de conexión a SQL Server (ajusta estos valores)
      - DB_DRIVER=com.microsoft.sqlserver.jdbc.SQLServerDriver
      - TZ=Europe/Madrid
      - JAVA_OPTS=-Duser.timezone=Europe/Madrid
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 5

# No incluyas el servicio sqlserver-dev si ya existe como contenedor externo
networks:
  cuba-network:
    external: true